import React from "react";
import RespondedOpenToWorkResquets from "./Responded";

import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Responded Open To Work",
};

const page = () => {
  return <RespondedOpenToWorkResquets />;
};

export default page;
