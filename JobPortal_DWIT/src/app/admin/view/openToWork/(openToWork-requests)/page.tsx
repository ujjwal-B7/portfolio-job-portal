import React from "react";
import OpenToWorkPage from "./OpenToWork";

import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Open To Work",
};

const page = () => {
  return <OpenToWorkPage />;
};

export default page;
